# homebrew-ww

Homebrew tap for [`ww`](https://github.com/yoskeoka/ww), a workspace-aware git worktree manager.

## Install

```bash
brew tap yoskeoka/ww
brew install ww
```

## Maintenance

Formula updates in `Formula/` are intended to be managed by GoReleaser from the main `ww` repository.
